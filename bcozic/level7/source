#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

char	c[64];

void	m(void)
{
	printf("%s - %d\n", c, time(0));
}

int		main(int ac, char **av)
{
	char	**str;
	char	**str2;

	str = malloc(8);
	*str = (char*)1;
	str[1] = malloc(8);
	str2 = malloc(8);
	*str2 = (char*)2;
	str2[1] = malloc(8);
	strcpy(str[1], av[1]);
	strcpy(str2[1], av[2]);
	fgets(c, 68, fopen("/home/user/level8/.pass", "r"));
	puts("~~");
	return (0);
}
