int main(int ac, char **av)
{
	FILE	*file;
	char	c;
	char	str[33];

	file = fopen("/home/user/end/.pass", "r");
	memset(str, 0, 33);
	if (file == 0)
		return (-1);
	if (ac != 2)
		return (-1);
	fread(str, 1, 66, file);
	char c = 0;
	str[atoi(av[1])] = 0;
	*(int*)str = file;
	fread(str + 66, 1, 65, *(int*)str);
	fclose(file);
	if (strcmp(str, av[1]) == 0)
		execl("/bin/sh", "sh", 0);
	else
		puts(str + 66);
	return (0);
}
